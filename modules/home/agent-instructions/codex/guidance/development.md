# Development routing — Codex adapter

Use the relevant section for tool selection, navigation, Python environments, or version-control conventions.

## Codex tools

- **Search**: The Search section of `~/.codex/AGENTS.md` names the tool for each case.
- **Read**: Use `bat -pp` for focused reads.
- **Review**: Use `calldiff` for call structure, `hunk` for human review, and `glimpse-changes` for walkthroughs.
- **GitHub**: Use `better-github-skill` for PR state and `gh` for other GitHub work.
