# Enter the repo's pinned maintainer shell (flake devShells.default) via
# direnv + nix-direnv. Run `direnv allow` once to opt in.
export NIX_CONFIG="substituters = https://cache.nixos.org https://claude-code.cachix.org"
use flake
