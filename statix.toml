# statix lint configuration (used by `just lint`).
# references/ holds third-party sample repos that are never imported by the
# flake; linting them is noise.
disabled = []
ignore = ["references", ".direnv"]
